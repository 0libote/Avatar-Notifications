{
  "name": "notifbuddy",
  "private": true,
  "version": "0.1.0",
  "description": "NotifBuddy — a cute pixel pal that watches your Windows notifications so you never miss one.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.3",
    "vite": "^6.0.3"
  }
}
